# Establece el algoritmo de escalado
# scaler={simple,nearestneighbor,bilinear,bell,bspline,quadratic,gaussian,mitchell,catmullrom,lanczos,blackman,kaiser}
#
# Por omisi¢n se utiliza el algoritmo ÆsimpleØ. êste es el m†s r†pido, aunque
# la calidad resultante es aceptable para un visionado normal.
scaler=simple

# Establece el n£mero de p†ginas de los archivos multi-imagen que se deber°an
# preprocesar en segundo plano para dibujarlas m†s r†pido (a costa de consumir
# m†s memoria). Siempre se generar†n en segundo plano <la mitad de> p†ginas por
# delante y por detr†s de la actual.
progressive_render_pages=2
